@model IEnumerable<Rocy.Models.ApplicationType>
@{
    ViewData["Title"] = "Category";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6 ">
            <h3 class="text-primary">Application Types List</h3>
        </div>
        <div class="col-6">
            <a asp-Controller="ApplicationType" asp-action="Create" class="btn btn-primary">
                Create New Application type
            </a>

        </div>
    </div>
</div>
<br /><br />

@if (Model.Count() > 0)
{
    <table class="table table-striped table-bordered">
        <thead>
            <th>Name</th>
            <th></th>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    <td></td>
                </tr>
            }
        </tbody>
    </table>
}
